APP_URL=http://localhost
APP_PORT=8080
DOMAIN_BASE=pets.local
ACME_EMAIL=my@email.com

# Ethereum settings
ETH_NETWORK=sepolia

# Session cookie signing key (set a long random value in production).
# If empty, an ephemeral key is generated at startup and sessions reset on restart.
SESSION_KEY=
